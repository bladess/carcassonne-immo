{% extends 'base.html.twig' %}

{% block body %}
    <h1>Annonce</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ annonce.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ annonce.description }}</td>
            </tr>
            <tr>
                <th>Prix </th>
                <td>{{ annonce.prix }} €</td>
            </tr>
            <tr>
                <th>Nbpieces</th>
                <td>{{ annonce.nbPieces }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td><img src="{{ asset('uploads/photos/' ~ annonce.photo) }}" alt='photo'></td>
            </tr>
            <tr>
                <th>Type d'annonce </th>
                <td>{{ annonce.typeAnnonce }}</td>
            </tr>
            <tr>
                <th>Admin ayant saisie l'annonce </th>
                <td>{{ annonce.administrateur }}</td>
            </tr>
            <tr>
                <th>Client </th>
                <td>{{ annonce.client }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_annonce_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_annonce_edit', { 'id': annonce.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
